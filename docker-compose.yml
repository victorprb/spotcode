version: '3'
services:
  web:
    build: .
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    depends_on:
      - db

  db:
    image: postgres:11.6-alpine
    volumes:
      - postgresql_data

volumes:
  postgresql_data: